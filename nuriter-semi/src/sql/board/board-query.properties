insertReportNuri=INSERT INTO BOARD VALUES (BOARD_SEQ.NEXTVAL, ?, ? , ?,DEFAULT, SYSDATE,'REPORT',NULL, ?, 'Y')
reportListCount=SELECT COUNT(*) FROM BOARD
selectReportList=SELECT RNUM, BOARD_TITLE, BOARD_CONTENT, USER_NUMBER, BOARD_DATE, OWNER_NUMBER FROM(SELECT ROWNUM RNUM, BOARD_TITLE, BOARD_CONTENT, USER_NUMBER, BOARD_DATE, N.OWNER_NUMBER FROM BOARD B JOIN NEWNURI N ON (N.NURI_NUMBER = B.NURI_NUMBER) WHERE BOARD_SORT = 'REPORT') WHERE RNUM BETWEEN ? AND ?